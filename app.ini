[uwsgi]
module = app:app
callable = app
virtualenv = /media/jemy/Data/Linux Adminstration/Github Projects/saraya-project/jemy/HiveBox-Scalable-RESTful-API-for-Beekeepers/env
master = true
processes = 4
socket = /tmp/app.sock
chmod-socket = 660
vacuum = true
die-on-term = true
uid = www-data  # Ensure this matches the user Nginx runs as
gid = www-data  # Ensure this matches the group Nginx runs as

